LOOK & FEEL ASSETS — filenames (no spaces)
==========================================

Use these exact WebP names in `public/assets/look-feel/`:

  hero.webp
  section-2.webp   → intro (gray band, right column)
  section-3.webp   → lifestyle banner
  section-4.webp   → spare / swap in lookFeel.ts if needed
  section-5.webp   → why us (silhouette) column

Config: `src/config/lookFeel.ts` → `lookFeelBasenames`.

Hero logo: text-only while `logoLightUrl` is empty in `src/config/brand.ts`.
